"""
الإعدادات العامة والثوابت لنظام إدارة المركز الثاني للمستجدين (V3).
كل المسارات نسبية لمجلد المشروع حتى يعمل البرنامج بعد تحويله EXE.
"""
from __future__ import annotations
import os
import sys

# ---------------------------------------------------------------------------
# تحديد مجلد التطبيق (يدعم التشغيل العادي وبعد التحويل إلى EXE بـ PyInstaller)
# ---------------------------------------------------------------------------
def app_dir() -> str:
    """يرجّع المجلد الذي يعمل منه التطبيق."""
    if getattr(sys, "frozen", False):
        # عند التشغيل كـ EXE
        return os.path.dirname(sys.executable)
    # عند التشغيل العادي: مجلد markaz2 (الأب لمجلد core)
    return os.path.dirname(os.path.dirname(os.path.abspath(__file__)))


BASE_DIR = app_dir()

DATA_DIR = os.path.join(BASE_DIR, "data")
BACKUP_DIR = os.path.join(BASE_DIR, "backups")
EXPORT_DIR = os.path.join(BASE_DIR, "exports")
ASSETS_DIR = os.path.join(BASE_DIR, "assets")

DB_PATH = os.path.join(DATA_DIR, "markaz2.db")

# إنشاء المجلدات إن لم تكن موجودة
for _d in (DATA_DIR, BACKUP_DIR, EXPORT_DIR, ASSETS_DIR):
    os.makedirs(_d, exist_ok=True)

# ---------------------------------------------------------------------------
# ثوابت العمل (Domain constants)
# ---------------------------------------------------------------------------

# كل شهر قبض = 30 يوم ثابت (قاعدة الماهية)
DAYS_PER_PAY_MONTH = 30

# الدفعات الأربعة في السنة
INTAKE_MONTHS = {
    1: "يناير",
    4: "أبريل",
    7: "يوليو",
    10: "أكتوبر",
}

# المؤهلات
QUALIFICATIONS = ["عليا", "فوق متوسط", "متوسط", "عادة"]

# العهد
CUSTODY_TYPES = ["ميري", "ملكي", "ميري كامل"]

# سعة نماذج الصرفية (عدد الأسماء)
SARFEYA_CAPACITY = {
    "صغيرة": 6,
    "متوسط": 20,
    "كبيرة": 50,
}

# قائد المركز (للتوقيع في النماذج الرسمية)
CENTER_COMMANDER = "عقيد/ أمجد العشري"
CENTER_NAME = "المركز الثاني للمستجدين"
CENTER_DEPARTMENT = "الإدارة العامة لتدريب قوات الأمن"

# ---------------------------------------------------------------------------
# قاعدة الكتيبة: المحافظة -> رقم الكتيبة (تلقائية في كل التابات)
# ---------------------------------------------------------------------------
GOVERNORATE_TO_BATTALION = {
    # كتيبة 1
    "دمياط": 1, "الدقهلية": 1, "كفرالشيخ": 1, "كفر الشيخ": 1, "بورسعيد": 1,
    "الشرقية": 1, "الاسماعيلية": 1, "الإسماعيلية": 1, "السويس": 1,
    "شمال سيناء": 1, "جنوب سيناء": 1,
    # كتيبة 2
    "الاسكندرية": 2, "الإسكندرية": 2, "البحيرة": 2, "الغربية": 2,
    "مطروح": 2, "المنوفية": 2,
    # كتيبة 3
    "القاهرة": 3, "القليوبية": 3, "الجيزة": 3, "الفيوم": 3,
    # كتيبة 4
    "قنا": 4, "اسوان": 4, "أسوان": 4, "بني سويف": 4, "البحر الاحمر": 4,
    "البحر الأحمر": 4, "الاقصر": 4, "الأقصر": 4, "المنيا": 4, "اسيوط": 4,
    "أسيوط": 4, "سوهاج": 4, "الوادي الجديد": 4,
}


def battalion_for(governorate: str) -> int:
    """يرجّع رقم الكتيبة من اسم المحافظة، أو 0 لو غير معروفة."""
    if not governorate:
        return 0
    return GOVERNORATE_TO_BATTALION.get(governorate.strip(), 0)


# قائمة المحافظات (للقوائم المنسدلة في الواجهة)
GOVERNORATES = [
    "القاهرة", "الجيزة", "القليوبية", "الاسكندرية", "البحيرة", "الغربية",
    "المنوفية", "الدقهلية", "كفرالشيخ", "دمياط", "الشرقية", "بورسعيد",
    "الاسماعيلية", "السويس", "شمال سيناء", "جنوب سيناء", "مطروح",
    "الفيوم", "بني سويف", "المنيا", "اسيوط", "سوهاج", "قنا", "الاقصر",
    "اسوان", "البحر الاحمر", "الوادي الجديد",
]
